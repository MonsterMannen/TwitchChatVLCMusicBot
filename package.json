{
  "name": "TwitchChatVlc",
  "productName": "TwitchChatVlc",
  "version": "2.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-packager . twitch-chat-vlc --overwrite --asar --platform=win32 --arch=ia32 --icon=icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName='Twitch Chat Vlc'",
    "zip": "7za a release-builds/twitch-chat-vlc.zip release-builds/*"
  },
  "author": "MonsterMannen",
  "license": "ISC",
  "dependencies": {
    "electron": "^4.0.7",
    "twitch-bot": "^1.2.5",
    "youtube-finder": "^1.0.1"
  },
  "devDependencies": {
    "electron-packager": "^13.1.1"
  }
}
